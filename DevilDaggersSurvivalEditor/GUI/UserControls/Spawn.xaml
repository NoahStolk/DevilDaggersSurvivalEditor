<UserControl x:Class="DevilDaggersSurvivalEditor.GUI.UserControls.SpawnControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="400">
	<Grid x:Name="Grid">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="3*" />
			<ColumnDefinition Width="3*" />
		</Grid.ColumnDefinitions>
		<Label Grid.Column="0" Content="{Binding ID}"></Label>
		<Label Grid.Column="1" Content="{Binding Seconds}"></Label>
		<Label Grid.Column="2" Content="{Binding EnemyName}"></Label>
		<Label Grid.Column="3" Content="{Binding Delay}"></Label>
		<Label Grid.Column="4" Content="{Binding NoFarmGems}"></Label>
		<Label Grid.Column="5" Content="{Binding TotalGems}"></Label>
	</Grid>
</UserControl>