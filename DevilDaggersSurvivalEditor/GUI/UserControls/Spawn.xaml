<UserControl x:Class="DevilDaggersSurvivalEditor.GUI.UserControls.SpawnControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="400">
	<Grid x:Name="Grid" Width="652" Height="20">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="1*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="2*" />
			<ColumnDefinition Width="3*" />
			<ColumnDefinition Width="3*" />
		</Grid.ColumnDefinitions>
		<Label Padding="2,2,2,2" Grid.Column="0" Content="{Binding ID}" />
		<Label Padding="2,2,2,2" Grid.Column="1" Content="{Binding Seconds}" />
		<Label Padding="2,2,2,2" Grid.Column="2" Content="{Binding EnemyName}" Name="Enemy" />
		<Label Padding="2,2,2,2" Grid.Column="3" Content="{Binding Delay}" />
		<Label Padding="2,2,2,2" Grid.Column="4" Content="{Binding NoFarmGems}" />
		<Label Padding="2,2,2,2" Grid.Column="5" Content="{Binding TotalGems}" />
	</Grid>
</UserControl>